{
  "name": "arabic-calligraphy",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Arabic calligraphy drawing app",
  "scripts": {
    "build": "node scripts/generate-worker.mjs && mkdir -p dist/.openai && cp .openai/hosting.json dist/.openai/"
  },
  "files": [
    "dist"
  ]
}
