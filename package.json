{
  "name": "arabic-calligraphy",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Arabic calligraphy drawing app",
  "scripts": {
    "build": "mkdir -p dist dist/server && cp index.html styles.css app.js brush.js forms.js glyph.js keyboard.js letters.js styles.js manifest.webmanifest dist/ && cp -r assets dist/ && cp server/index.js dist/server/ && cp .openai/hosting.json dist/"
  },
  "files": [
    "dist"
  ]
}
